import React from "react";

const UnableFetch = () => {
  return (
    <h2
      style={{
        color: "var(--text-color)",
        textAlign: "center",
      }}
    >
      Unable to fetch facts, check your internet connection!
    </h2>
  );
};

export default UnableFetch;
